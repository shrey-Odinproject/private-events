<h1>Event Details</h1>
<h2>Title: <%= @event.title %></h2>

<p>Location: <%= @event.location %></p>
<p>Date: <%= @event.date %></p>

<%# unless current_user.created_events.include? @event | This logic also works too %>
<% if current_user != @event.creator%><%# if the cretor of the event u are viewing is not current_user (you) then u can be attendee%>
  BUTTON TO ATTEND Event
<% end %>

<%=link_to 'logout', destroy_user_session_path,data: { turbo_method: :delete } %><br><br>
<%= link_to 'create-events',new_event_path %> <br><br>
<%= link_to 'user created events',user_path(current_user) %> <br><br>